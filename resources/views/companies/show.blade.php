@extends('layouts.app')
@section('title',$company->name)
@section('content')
<section class="relative bg-gradient-to-r from-secondary to-[#4f2933] text-white"><div class="mx-auto max-w-[1280px] px-5 py-20 lg:px-10"><div class="flex flex-col gap-7 md:flex-row md:items-end"><div class="grid size-28 place-items-center rounded-3xl bg-white font-display text-4xl font-bold text-primary shadow-xl">{{ strtoupper(substr($company->name,0,1)) }}</div><div><span class="rounded-full bg-[#007d7d] px-4 py-2 text-xs font-bold">TERVERIFIKASI</span><h1 class="mt-5 font-display text-4xl font-bold lg:text-6xl">{{ $company->name }}</h1><p class="mt-3 text-white/70">{{ $company->industry ?: 'General' }} &middot; {{ $company->city }}</p></div></div></div></section>
<section class="mx-auto grid max-w-[1280px] gap-8 px-5 py-14 lg:grid-cols-[1fr_340px] lg:px-10"><div><div class="card"><h2 class="font-display text-2xl font-bold">Tentang perusahaan</h2><p class="mt-5 leading-8 text-on-surface-variant">{{ $company->description ?: 'Perusahaan ini telah diverifikasi CasualHub dan berkomitmen menyediakan lingkungan kerja casual yang aman dan profesional.' }}</p></div><h2 class="mt-12 font-display text-2xl font-bold">Lowongan aktif</h2><div class="mt-5 grid gap-4 sm:grid-cols-2">@forelse($company->jobs as $job)<a href="{{ route('jobs.show',$job) }}" class="rounded-3xl bg-white p-6 shadow-sm hover:-translate-y-1"><span class="badge">{{ $job->category?->name }}</span><h3 class="mt-4 font-display text-lg font-semibold">{{ $job->title }}</h3><p class="mt-2 text-sm text-on-surface-variant">{{ $job->location }}</p><b class="mt-5 block">Rp{{ number_format($job->daily_rate,0,',','.') }}/hari</b></a>@empty<p class="text-on-surface-variant">Belum ada lowongan aktif.</p>@endforelse</div></div><aside class="card h-fit"><h3 class="font-display text-xl font-bold">Informasi</h3><dl class="mt-6 space-y-5 text-sm"><div><dt class="text-on-surface-variant">Alamat</dt><dd class="mt-1 font-semibold">{{ $company->address ?: $company->city }}</dd></div><div><dt class="text-on-surface-variant">Website</dt><dd class="mt-1 font-semibold">{{ $company->website ?: 'Belum tersedia' }}</dd></div><div><dt class="text-on-surface-variant">Total lowongan</dt><dd class="mt-1 font-semibold">{{ $company->jobs_count }}</dd></div></dl></aside></section>
@endsection
