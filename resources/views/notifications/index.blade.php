@extends('layouts.app')
@section('title','Notifikasi')
@section('content')
<section class="mx-auto max-w-4xl px-5 py-16"><div class="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p class="text-xs font-bold uppercase tracking-[.2em] text-primary">Updates</p><h1 class="mt-3 font-display text-4xl font-bold">Notifikasi</h1></div><form method="POST" action="{{ route('notifications.read-all') }}">@csrf<button class="btn-ghost" type="submit">Tandai semua dibaca</button></form></div><div class="mt-10 overflow-hidden rounded-3xl bg-white shadow-sm">@forelse($notifications as $notification)<a href="{{ data_get($notification->data,'url','#') }}" class="flex gap-4 border-b border-black/5 p-6 last:border-0 {{ !$notification->read_at?'bg-primary-soft/50':'' }}"><div class="grid size-12 shrink-0 place-items-center rounded-full bg-primary-soft text-primary"><x-icon name="notifications" /></div><div><h2 class="font-semibold">{{ data_get($notification->data,'title','Pembaruan CoC') }}</h2><p class="mt-2 text-sm leading-6 text-on-surface-variant">{{ data_get($notification->data,'message','Ada pembaruan baru pada akunmu.') }}</p><p class="mt-2 text-xs text-neutral">{{ $notification->created_at->diffForHumans() }}</p></div></a>@empty<div class="p-20 text-center"><x-icon name="notifications" class="mx-auto size-12 text-primary"/><h2 class="mt-5 font-display text-2xl font-bold">Belum ada notifikasi</h2><p class="mt-2 text-on-surface-variant">Pembaruan akun dan pekerjaan akan tampil di sini.</p></div>@endforelse</div><div class="mt-8">{{ $notifications->links() }}</div></section>
@endsection

