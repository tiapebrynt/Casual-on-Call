@extends('layouts.app')
@section('title','Reviews & Ratings')
@section('content')
<section class="mx-auto max-w-[1100px] px-5 py-16 lg:px-10"><div class="grid gap-8 lg:grid-cols-[1fr_360px]"><div><p class="text-xs font-bold uppercase tracking-[.2em] text-primary">Reputation</p><h1 class="mt-3 font-display text-4xl font-bold">Reviews & Ratings</h1><p class="mt-3 text-on-surface-variant">Ulasan terverifikasi dari pekerjaan yang telah diselesaikan.</p></div><div class="card text-center"><strong class="font-display text-6xl">{{ number_format($average,1) }}</strong><div class="mt-3 text-xl tracking-widest text-primary">&#9733;&#9733;&#9733;&#9733;&#9733;</div><p class="mt-2 text-sm text-on-surface-variant">{{ $reviews->total() }} ulasan</p></div></div><div class="mt-12 grid gap-5 md:grid-cols-2">@forelse($reviews as $rating)<article class="card"><div class="flex items-start justify-between"><div class="flex items-center gap-3"><div class="grid size-11 place-items-center rounded-full bg-primary-soft font-bold text-primary">{{ strtoupper(substr($rating->reviewer->name,0,1)) }}</div><div><b>{{ $rating->reviewer->name }}</b><p class="text-xs text-on-surface-variant">{{ $rating->created_at->format('d M Y') }}</p></div></div><span class="font-bold text-primary">{{ $rating->score }} &#9733;</span></div><h2 class="mt-6 font-display text-lg font-semibold">{{ $rating->review?->title ?: $rating->application->job->title }}</h2><p class="mt-3 leading-7 text-on-surface-variant">{{ $rating->review?->body ?: 'Rating diberikan setelah pekerjaan berhasil diselesaikan.' }}</p></article>@empty<div class="card col-span-full py-20 text-center"><h2 class="font-display text-2xl font-bold">Belum ada ulasan</h2><p class="mt-2 text-on-surface-variant">Ulasan muncul setelah pekerjaan selesai.</p></div>@endforelse</div><div class="mt-10">{{ $reviews->links() }}</div></section>
@endsection

